combo
.PP
This command turns on the combinational optimization.  When a cover of all
the set (or reset) regions completely covers all excited and quiescent state,
then the state-holding element is not necessary and the gate can be 
implemented combinationally.
 