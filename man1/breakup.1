breakup <filename>
.PP
This function breaks up gates which are larger than the
maximum fanin size (DEFAULT = 4).  The new specification
in which extra signals have been added to breakup gates
is stored to a file named <filename>BRK.er and it is
immediately loaded.
