findviol <filename>
.PP
This function finds all violations and constructs context rule
tables to solve them.  The result is stored to the file named
<filename>.crt which is in the following form:
.PP
Context Rule Table for <event>
.br
<problem state> : list of enabling events for context rules
