findtd <filename>
.PP
This function finds all the possible time differences for the
given number of cycles, and it outputs the results to a file
named <filename>.td in the following form:
.PP
t(< v,i >) - t(< u,i-j >) = [ Li,Ui ]
