#include "struct.h"

bool load_extended_burstmode_graph(designADT design);  /*** Reads in an extended burstmode file and translates ***/
                                                       /*** it into a compacted state graph.                   ***/

